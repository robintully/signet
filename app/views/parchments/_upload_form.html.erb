  <%= form_for [@envelope, Parchment.new], :html => {:multipart => true} do |f| %>

  <%= f.file_field :file,multiple: true, name: "attachment[file][]", onchange: "validateFiles(this);",
    data: { max_file_size: 5.megabytes } %>
  <%= f.submit "Save Envelope", class: "btn btn-default" %>
  <% end %>

